name: Test Self-Hosted Runner 'runner-sv'

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  my_sv_job:
    runs-on: runner-sv 
    
    steps:
      - name: 👋 Test Runner
        run: echo "Job is running on the runner named 'runner-sv'."